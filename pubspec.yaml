name: badwallet_app
description: "Application mobile d'entreprise Consumer pour BadWallet"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0 
  provider: ^6.1.1 
  intl: ^0.19.0 
  google_fonts: ^6.1.0 
  flutter_secure_storage: ^9.0.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1 

flutter_icons:
  android: "launcher_icon"
  image_path: "assets/icon/icon.png"

flutter:
  uses-material-design: true